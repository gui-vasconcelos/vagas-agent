# Setup for Other Users

The full onboarding guide moved to the repository README:

→ **[/README.md](../README.md)**

It covers the three accounts to create (GitHub, DeepSeek, Resend — with exact
steps and API key creation), adapting `config.yaml` and the classifier prompt,
GitHub secrets, first run, local testing, tuning, and known pitfalls.
